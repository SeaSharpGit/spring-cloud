server:
  port: 10000

spring:
  profiles:
    active: dev
  application:
    name: @artifactId@
  cloud:
    nacos:
#      discovery:
#        server-addr: ${NACOS-HOST:127.0.0.1}:${NACOS-PORT:8848}
      config:
        server-addr: ${NACOS-HOST:127.0.0.1}:${NACOS-PORT:8848}
        file-extension: yml


